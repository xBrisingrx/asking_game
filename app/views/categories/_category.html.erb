<tr>
  <td><%= category.name %></td>
  <td>
    <% if category.image.attached? %>
      <%= image_tag(category.image, size: "50X100", alt: "Category image") %>
    <% end %>
  </td>
  <td> <%= link_to 'Editar', edit_category_path(category), class: "btn btn-warning" %> </td>
</tr>